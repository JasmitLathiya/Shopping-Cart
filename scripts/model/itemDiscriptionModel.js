let itemDiscriptionModel = {
    init : function(name,color,size)
    {
        this.data = {
            "name" : name,
            "color" : color,
            "size" : size
        }
    }
}

export {itemDiscriptionModel}